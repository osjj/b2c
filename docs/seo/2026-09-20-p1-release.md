# 2026-09-20 SEO P1 更新交接

四项 P1 的目标数据库内容已更新并回读核对。服务器代码仍由你按现有流程手动部署；部署并确认页面显示新内容后，再向 GSC 提交更新。

## 本次完成的内容

| P1 页面 | 更新内容 |
|---|---|
| 建筑呼吸防护文章 | 增加 NIOSH 标签识别、官方数据库查询、完整组件配置核对和采购清单；同步该文章 SEO 元数据。 |
| 建筑 OSHA PPE 文章 | 增加工地 PPE 要求张贴位置的解释、Subpart E 导航和采购交接检查；区分法规要求与现场建议；同步该文章 SEO 元数据。 |
| 割草网状面屏商品 | 更新 8 个公开内容/SEO 字段，补充选型、使用边界、替换面网、FAQ 和询价说明。净重按你确认的 **215 g** 统一；原 **88 g** 包装毛重已移除，包装后毛重仍须报价前确认。 |
| 仓储 PPE 主方案 | 整理为 13 个内容区块，增加尺码、发放、补货和文件核查内容；停止不合适的自动商品推荐，改用 5 个分类入口；新增仓储 PPE 清单 CSV 和对应下载资源。 |

保留原 URL、文章发布状态和发布日期。商品图库及详情的 7 张图片均保留，价格、库存、变体和独立物流重量字段未改。仓储相近方案未合并，其他方案和既有商品关联未删除。

## 需要随服务器代码一起部署的文件

- `construction-respiratory-protection.md`
- `osha-ppe-requirements-construction.md`
- `prisma/seed-blog.ts`：两篇文章的元数据，以及本轮页内锚点链接处理。
- `prisma/solutions-data.json`：目标仓储方案的内容。
- `src/app/(store)/solutions/[slug]/page.tsx`：目标仓储方案的下载资源配置。
- **`public/downloads/warehouse-ppe-checklist.csv`：新增文件，必须一并复制。** 包含 30 条数据、7 列，范围是 PPE 选型、发放和采购文档核查。
- `prisma/product-content-updates/2026-09-20-wire-mesh-face-shield.json`：本次商品定向更新的归档记录。
- `tsconfig.json`：排除 `output`、`outputs` 生成资料目录，避免旧临时上传脚本进入应用构建。

按你原有的服务器部署方法更新代码、安装所需依赖、构建并重启应用。本次没有新增依赖或数据库迁移。**目标数据库已同步，无须重新运行全量 blog seed、方案同步或商品导入。** 商品归档 payload 也无需再次执行，其 guard 对应本次更新前的记录。

## 当前核对状态

- 数据库定向更新及回读通过，受保护字段、原图片和其他方案的保留检查通过。
- 最终本地生产预览的 4 个页面均返回 HTTP 200，全部新增标记可见、canonical 正确且为 `index, follow`；CSV 返回 `200 text/csv; charset=UTF-8`，30 条数据核对通过。
- 新增文章锚点与实际标题生成规则已核对，仓储 CSV 可以解析为 30 条数据、7 列。
- 生产构建通过；目标 ESLint、6 个既有方案分类/推荐测试和实际 Markdown 解析函数的隔离验证通过。
- 两篇文章已重新定向同步，4 个页内快捷链接保留同页行为；浏览器已实际点击验证两篇文章的新章节跳转，并目视检查四个页面的新内容。仓储页无横向溢出，主按钮指向联系入口、次按钮指向产品列表，新增 CSV 下载卡片显示正常。
- 仓储页 5 个分类、2 个工具、联系页和产品列表共 9 个目标链接在线返回 HTTP 200。
- 独立 `npx tsc --noEmit` 仍报告 5 个既有测试错误，位于 `src/lib/body-link-map.test.ts` 和 `src/lib/embeddings.test.ts`；这两个测试及其对应实现均与 HEAD 相同。本次未改这些文件。Next 的生产构建过滤测试文件诊断，因此不能把构建通过表述为全仓 TypeScript 检查通过。
- 未执行 Git 提交、推送、服务器部署、GSC 提交或 IndexNow 通知。

2026-09-20 **17:33（北京时间，09:33 UTC）** 的线上 HTTP 核对：商品页已显示新内容和 215 g，原 88 g 不再显示；两篇文章和仓储方案尚未显示本轮新增标记；新 CSV 返回 404。这是该时点的线上结果，不能用数据库已更新代替部署后验证。

## 部署后确认，再提交 GSC

| 提交的正式网址 | 在页面中确认的内容 |
|---|---|
| [https://www.laifappe.com/blog/construction-respiratory-protection](https://www.laifappe.com/blog/construction-respiratory-protection) | `Check the NIOSH Approval Before Ordering`；点击页内链接能跳到该节。 |
| [https://www.laifappe.com/blog/osha-ppe-requirements-construction](https://www.laifappe.com/blog/osha-ppe-requirements-construction) | `Where Are a Worksite's Minimum PPE Requirements Posted?`；点击开头的导航能跳到对应内容。 |
| [https://www.laifappe.com/products/wire-mesh-grass-trimmer-face-shield](https://www.laifappe.com/products/wire-mesh-grass-trimmer-face-shield) | `Choose your mesh face shield configuration`、`Net product weight is 215 g.`；毛重标注待确认。 |
| [https://www.laifappe.com/solutions/ppe-safety-equipment-for-warehouse-logistics](https://www.laifappe.com/solutions/ppe-safety-equipment-for-warehouse-logistics) | `Plan PPE Issue, Sizes and Replenishment`、`Documents to Check Before Bulk Approval`；仓储清单下载入口正常。 |

另外打开[仓储 PPE 清单 CSV](https://www.laifappe.com/downloads/warehouse-ppe-checklist.csv)，确认能下载真正的 CSV，而不是 404 页面。四个页面应正常返回，canonical 指向上表对应网址且没有 `noindex`。确认这些内容后，再逐个到 GSC 的网址检查中请求编入索引。

## 本地证据与恢复边界

本地证据目录：`D:/project/b2c-store/output/seo-p1-20260920/`。其中保留更新前快照、应用结果、数据库回读、线上检查和商品内容校验。**这些文件含内部业务数据，不要发布到网站或复制到公开下载目录。**

如需恢复，应先核对目标记录当前更新时间，仅使用备份恢复本次对应字段；不要覆盖之后的新修改，也不要全库还原。服务器代码恢复与数据库内容恢复需要分别处理。
